apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - configmap.yaml
  - rbac.yaml
  - job.yaml
# Supply repair-engine-database from a secret manager; secret.example.yaml is not applied.
